kind = "web"
previewPath = "/"
title = "imdanang — Da Nang Tourism"
version = "1.0.0"
id = "artifacts/imdanang"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21082

[services.development]
run = "pnpm --filter @workspace/imdanang run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/imdanang", "run", "build" ]
publicDir = "artifacts/imdanang/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21082"
BASE_PATH = "/"
